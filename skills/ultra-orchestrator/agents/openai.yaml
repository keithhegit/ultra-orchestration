interface:
  display_name: "Ultra Orchestrator"
  short_description: "Main strict Ultra control plane"
  default_prompt: "Use $ultra-orchestrator to choose run mode, prefer strict OpenSpec control for development, and route through ledger-backed planning, execution, review, QA, and delivery."

policy:
  allow_implicit_invocation: true
