# codex_ultra_orchestration

This subdirectory collects the artifacts produced for this thread as a standalone project workspace.

## Contents

- `docs/`
  - architecture whitepapers in English and Chinese
- `examples/`
  - sample intake and run output artifacts
- `runs/`
  - initialized run directories and ledger examples

## Scope

This folder is for thread-specific project artifacts.

The reusable skills remain under `.agents/skills/` because they are workspace-level agent capabilities rather than project documents.
