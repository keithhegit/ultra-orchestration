# Teammate Pilot Notes

The `/teammate` repository already demonstrates the intended OpenSpec shape:

- `openspec/specs/` for long-lived program assets
- `openspec/changes/<change-id>/` for bounded changes
- `ultra-bridge.md` as an optional execution mapping artifact

This makes `/teammate` a strong pilot target for bridge validation because the
spec layer already exists and the next milestones are architecture-heavy.
